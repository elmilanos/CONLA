<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login CONLA</title>
</head>
<body>
    <% if (signupMsg) { %>

        <div>   <%= signupMsg %>    </div>
    
    <% } %>
    
    <% if (signinMsg) { %>

        <div>   <%= signinMsg %>    </div>
    
    <% } %>

    <%- body %>
</body>
</html>